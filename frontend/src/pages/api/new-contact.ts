import { supabase } from '@/db/supabase';
import type { APIRoute } from 'astro';

export const POST = async ({ request }) => {
	try {
		// This is where i will use the notion api to push potential leads
	} catch (error) {}
};
