---
const { name, href } = Astro.props;
import { Icon } from 'astro-icon/components';
---

<a
	class="border-2 text-xl border-indigo-500 block w-full p-4 rounded-full"
	href={href}>{name} <Icon class="inline" name="mdi:arrow-top-right" /></a
>
