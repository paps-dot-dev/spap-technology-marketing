---
const { name, href } = Astro.props;
import { Icon } from 'astro-icon/components';
---

<a
	class="border-2 text-xl border-white block w-full p-4 rounded hover:border-white hover:scale-110"
	href={href}>{name} <Icon class="inline" name="mdi:arrow-top-right" /></a
>
