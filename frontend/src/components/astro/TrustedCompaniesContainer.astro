---
import { supabase } from '../../db/supabase';

const { data, error } = await supabase.from('trusted_companies').select('*');

if (error) {
	throw new Error('Failed to fetch project' + error);
}
---

<div class="flex flex-wrap justify-between w-full items-center gap-8 p-5">
	{
		data.map((item) => (
			<div class=" flex-col items-center flex justify-center  ">
				<img class="max-w-16 md:max-w-24 grayscale" src={item.thumbnail_url} />
			</div>
		))
	}
</div>
